# Generated negative tests for Version

## Illegal transitions (must return 409 / be rejected)

- in state `draft`, event `approve` -> REJECT (no such transition)
- in state `draft`, event `requestChanges` -> REJECT (no such transition)
- in state `draft`, event `retire` -> REJECT (no such transition)
- in state `draft`, event `startReview` -> REJECT (no such transition)
- in state `submitted`, event `approve` -> REJECT (no such transition)
- in state `submitted`, event `delete` -> REJECT (no such transition)
- in state `submitted`, event `requestChanges` -> REJECT (no such transition)
- in state `submitted`, event `retire` -> REJECT (no such transition)
- in state `submitted`, event `submit` -> REJECT (no such transition)
- in state `reviewed`, event `delete` -> REJECT (no such transition)
- in state `reviewed`, event `retire` -> REJECT (no such transition)
- in state `reviewed`, event `startReview` -> REJECT (no such transition)
- in state `reviewed`, event `submit` -> REJECT (no such transition)
- in state `approved`, event `approve` -> REJECT (no such transition)
- in state `approved`, event `delete` -> REJECT (no such transition)
- in state `approved`, event `requestChanges` -> REJECT (no such transition)
- in state `approved`, event `startReview` -> REJECT (no such transition)
- in state `approved`, event `submit` -> REJECT (no such transition)
- in state `retired`, event `approve` -> REJECT (no such transition)
- in state `retired`, event `requestChanges` -> REJECT (no such transition)
- in state `retired`, event `retire` -> REJECT (no such transition)
- in state `retired`, event `startReview` -> REJECT (no such transition)
- in state `retired`, event `submit` -> REJECT (no such transition)
- in state `deleted`, event `approve` -> REJECT (no such transition)
- in state `deleted`, event `delete` -> REJECT (no such transition)
- in state `deleted`, event `requestChanges` -> REJECT (no such transition)
- in state `deleted`, event `retire` -> REJECT (no such transition)
- in state `deleted`, event `startReview` -> REJECT (no such transition)
- in state `deleted`, event `submit` -> REJECT (no such transition)

## Guard-denial tests (legal transition, guard false -> reject)

- `approved` --`retire`--> `retired` when NOT (status == approved) -> REJECT ("Only an approved version can be retired.")
- `retired` --`delete`--> `deleted` when NOT (refcount == 0) -> REJECT ("Cannot delete a version while an RFP still references it.")
- `draft` --`delete`--> `deleted` when NOT (refcount == 0) -> REJECT ("Cannot delete a version while an RFP still references it.")
